#pragma once

#include "wglad/wglad.h"

#define gl_is_bound(name, obj)          \
    glGetIntegerv(name, (obj))          \
    if(*(obj))                          \
    {                                   \
       bou \
    }                                   \


